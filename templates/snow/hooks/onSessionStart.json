{
  "onSessionStart": [
    {
      "description": "Provide bounded omp-flow mechanical orientation. Project Snow Hook rules replace same-event global rules; they are not merged.",
      "hooks": [
        {
          "type": "command",
          "command": "{{PYTHON_CMD}} .snow/hooks/session-start.py",
          "timeout": 10000,
          "enabled": true
        }
      ]
    }
  ]
}
