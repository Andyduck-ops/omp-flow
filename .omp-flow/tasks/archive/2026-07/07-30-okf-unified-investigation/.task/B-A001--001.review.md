# B-A001--001 Independent Re-review

Verdict: **PASS**

Reviewed work:

- [Switch Agent knowledge entry and navigation contracts](B-A001--001.implement.md)
- [Updated implementation handoff](B-A001--001.handoff.md)
- [Approved PRD](../prd.md)
- [Approved Design](../design.md)
- [Bundle entry interface](../context/interface/bundle-agent-entry.md)

## Findings

No blocking or substantive finding remains.

The previous review's sole blocker is closed across every canonical caller that owns native
assignment dispatch:

- `templates/.omp-flow/workflow.md` and `templates/common/skills/omp-flow/SKILL.md` make
  `operation start` the sole producer of the executable assignment.
- `templates/common/skills/omp-flow-execute/SKILL.md` preserves the complete assignment for
  implementation, review, and every batch item.
- `templates/common/skills/omp-flow-research/SKILL.md`,
  `templates/common/skills/omp-flow-design/SKILL.md`, and
  `templates/common/skills/omp-flow-qbd/SKILL.md` apply the same contract at their direct native
  dispatch sites.
- `templates/omp/agents/orchestrator.md` binds the executable OMP caller behavior.

Together these contracts require the returned assignment to be forwarded exactly unchanged, keep
the strict v1 `ompFlowDispatch` JSON as its first non-blank line, bind native item
`id = operation.actor_id = descriptor.actorId`, and keep the native role equal to the descriptor
role. They prohibit parsing, reserialization, added prose or instructions, inferred or dropped
fields, and partial reconstruction.

Batch dispatch starts one independent operation per item and preserves each
`(id = actorId, role, assignment)` tuple. Review predecessor receipt and predecessor output remain
inside the operation-produced descriptor. Brainstorm and decomposition do not directly dispatch a
native child, so they correctly inherit the router contract instead of duplicating a caller
protocol.

The wording matches the fail-closed receiver in `src/omp/extension.ts`: it parses only the first
non-blank line, validates the exact v1 key set, compares actor ID and role to the native item, and
checks receipt, entry, output, predecessor, and predecessor output against the active operation.

The repair is confined to B's canonical guidance/templates, corresponding deployed copies, and
handoff. It adds no Markdown parser, generated context fallback, legacy store fallback, or
B-owned runtime change. Runtime consumer work remains in the separately reviewed C scope.

## Independent verification evidence

1. Focused canonical caller-contract inspection — **PASS**, 7 caller files; no missing producer,
   unchanged-forwarding, first-non-blank, actor/role, batch, or predecessor-output obligation.
2. SHA-256 canonical/deployed comparison — **PASS**, 60 pairs across workflow, shared Skills, OMP
   agents, Codex agents, and Claude agents.
3. `npm run build` — **PASS**.
4. Python `tomllib` parsing of canonical and deployed Codex agent files — **PASS**, 10 files.
5. `git diff --check` over the B canonical/deployed scope — **PASS**; line-ending warnings only.
6. Isolated scratch executor assignment — **PASS**: strict v1 descriptor is the first assignment
   line and its role, actor ID, entry, output, objective, and receipt match the operation.
7. Isolated scratch second executor assignment — **PASS**: a distinct operation, actor ID,
   receipt, output, and unchanged assignment demonstrate per-item batch pairing.
8. Isolated scratch reviewer assignment — **PASS**: the descriptor carries the completed
   implementation predecessor and exact predecessor output from a different actor.
9. Static comparison with the OMP adapter's strict parser and validator — **PASS**: authored caller
   obligations match the executable receiver checks.

Total recorded checks: **9 run, 9 passed, 0 failed**.

## Remaining risk

No B-scope blocker remains. Broader deletion of transitional stores and legacy consumers is
deliberately owned by the dependent direct-cutover work, not by this navigation-contract row.
