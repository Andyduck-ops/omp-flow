# D-B001C001--001 implementation handoff

Status: DONE

## Outcome

The direct cutover is complete in canonical source and tracked native adapter copies. New task
knowledge is one Git-visible OKF v0.2 Bundle; the deployed Python surface is the six-file
mechanical runtime kernel plus `omp_flow.py`. No deployed or packaged producer/consumer remains
for lifecycle state, exact topology, Evidence, generated context, Reference slicing/digests,
gate phases, amendments, or their legacy Hook bridge.

This implements [the D work entry](D-B001C001--001.implement.md) after the accepted
[B handoff](B-A001--001.handoff.md) and [C handoff](C-A001--001.handoff.md).

## Changed and deleted inventory

### Runtime and installation

- Changed `templates/.omp-flow/scripts/common/{active_task,io,paths,task_store}.py`:
  removed legacy CSV/JSON lifecycle helpers, strengthened task path confinement, kept minimal
  Bundle scaffold/session/archive behavior, and made archive a same-filesystem rename.
- Kept `templates/.omp-flow/scripts/common/operation_store.py` and
  `templates/.omp-flow/scripts/omp_flow.py` as the A/C mechanical kernel.
- Deleted canonical legacy modules:
  `amend.py`, `context.py`, `currency.py`, `evidence.py`, `gates.py`, `reference.py`,
  `topology.py`, and `workflow.py`.
- Changed `src/cli/init.ts` so only `omp_flow.py`,
  `common/{__init__,active_task,io,operation_store,paths,task_store}.py`, and the final adapter
  resources deploy. Its one-time obsolete-resource list retires previously managed Python,
  Codex, Claude, Trellis, and old OMP-extension files during update; it is not a runtime reader.
- Added `templates/.omp-flow/gitignore`, deployed as `.omp-flow/.gitignore`.
- Changed `.gitignore`: task Bundles/archives are visible; `.runtime/` and
  `.omp-flow/cache/repos/` remain ignored.
- Changed `.npmignore` and `package.json` so the package ships the minimal runtime and excludes
  bytecode and the protected orphan described below.

### Native adapters and Hooks

- Deleted canonical and tracked deployed Codex workflow-state Hooks:
  `templates/codex/hooks.json`, `templates/codex/hooks/inject-workflow-state.py`,
  `.codex/hooks.json`, and `.codex/hooks/inject-workflow-state.py`.
- Simplified canonical and deployed Codex config to project document discovery only.
- Deleted canonical and deployed Claude semantic reconstruction/protection bridge:
  `_omp_core.py`, `inject-agent-context.py`, `inject-workflow-state.py`, and
  `protect-python-owned.py`.
- Replaced canonical and deployed Claude SessionStart with path-only runtime orientation; retained
  native identity injection; added `protect-runtime.py`, which protects only
  `.omp-flow/.runtime/` Write/Edit paths and allows normal Bundle Concept writing.
- Updated canonical/deployed Claude settings to those three Hook files and removed Agent/Task
  prompt replacement, per-turn state rendering, QbD digest exceptions, and broad `.omp-flow`
  mutation interception.
- Deleted active pre-cutover local consumers:
  `.omp/extensions/omp-flow/index.ts`, `.omp/agents/trellis-{research,implement,check}.md`,
  `.codex/hooks/session-start.py`, and the old non-`omp-flow-*` Codex workflow Skill cards
  (`before-dev`, `brainstorm`, `break-loop`, `check`, `check-cross-layer`, `create-command`,
  `finish-work`, `improve-ut`, `integrate-skill`, `onboard`, `record-session`, `start`,
  `update-spec`).

### Documentation, fixtures, and tests

- Rewrote `README.md` and `AGENTS.md` around the Bundle/mechanical boundary.
- Deleted the obsolete `docs/claude-adapter-verification.md` legacy Hook record.
- Deleted unused `templates/context/*.md.template` schema templates.
- Deleted the old Claude semantic Hook Python drivers and all
  `tests/fixtures/claude-hooks/**` payload/golden fixtures.
- Replaced `tests/omp-flow.test.ts` with 184 focused mechanical, semantic-dogfood, and native
  adapter-seam checks.

### Protected unrelated file

`templates/.omp-flow/scripts/common/disposition.py` had a pre-existing user modification before
this row. D did not overwrite or delete it. It is no longer imported, deployed, or packaged;
`src/cli/init.ts` treats an unchanged previously managed deployed copy as an obsolete update
target. Its remaining `doctor`/`dispose` text is therefore an orphaned local historical source,
not an active consumer or fallback.

The pre-existing deletions under `.omp-flow/knowhow/` and `.omp-flow/specs/` and other unrelated
working-tree changes were not used to widen this row and were left untouched.

## Isolated end-to-end dogfood

Scratch project:

`.omp-flow/scratch/d-row-dogfood-final-20260730`

Final archived Bundle:

`.omp-flow/tasks/archive/2026-07/07-30-okf-final-dogfood`

Observed semantic path:

- root `index.md` linked `brainstorm.md`, `research/runtime-boundary.md`, `design.md`,
  `qbd/human-decision.md`, `work/index.md`, and `review/runtime-review.md`;
- brainstorm linked research and research linked back, demonstrating the Explore spiral without
  promotion copies;
- `qbd/human-decision.md` explicitly approved the selected design and authored work grouping;
- `work/index.md` grouped `runtime-documentation.md` and `adapter-observation.md` in readable
  parallel prose without encoded IDs;
- implementation entry `work/runtime-documentation.md` produced linked
  `work/runtime-handoff.md`;
- independent review entered through the same work Concept, resolved the predecessor output, and
  wrote `review/runtime-review.md`;
- archive moved the whole Bundle and direct inspection confirmed all work/handoff/review relative
  targets still exist.

Mechanical observations:

- implementation actor: `dogfood-implementer`;
- implementation operation: `7b492574101149d3a97ff16ac4d02eed`;
- external/native receipt: `native-dogfood-implementation`;
- reviewer actor: `dogfood-reviewer`;
- review operation: `d7cf10b0f637468c971a708395365cf4`;
- review predecessor: `7b492574101149d3a97ff16ac4d02eed`;
- reviewer output resolved from the predecessor to
  `.omp-flow/tasks/07-30-okf-final-dogfood/work/runtime-handoff.md`;
- actors differ and the linked Review records that independence;
- archive destination:
  `.omp-flow/tasks/archive/2026-07/07-30-okf-final-dogfood`.

`git status --short -- .omp-flow/tasks` showed the Bundle/archive as visible. `git check-ignore`
matched `.omp-flow/.runtime` and `.omp-flow/cache/repos/...`. The installed update dry-run
reported all 73 managed resources unchanged and no obsolete path present.

The dogfood did not use a Markdown parser, fixed heading/list contract, context manifest,
Reference selector, persistent topology ID, CSV/JSONL ledger, or compatibility reader.

## Verification

- `python -X utf8 -m py_compile` over canonical runtime and final Claude Hooks — PASS.
- `npm run build` — PASS.
- `npm test` — PASS, 184 focused checks.
- `npm pack --dry-run --json` — PASS, 86 package files; zero legacy modules/Hooks,
  `disposition.py`, `__pycache__`, or `.pyc`.
- `git diff --check` — PASS; repository line-ending warnings only.
- isolated `node bin/omp-flow.js init --omp --codex --claude` — PASS.
- isolated `node bin/omp-flow.js update --dry-run` — PASS, all unchanged.
- isolated Bundle dogfood and archive — PASS.

## Final consumer search classification

No active Python, TypeScript adapter, Hook, Skill, agent card, package file, or test execution path
reads a retired format or command.

Remaining search matches are:

1. guardrail prose in `README.md`, `AGENTS.md`, `templates/.omp-flow/workflow.md`, and
   `omp-flow`/`omp-flow-decompose` Skills saying not to reintroduce retired formats;
2. negative scaffold assertions in `tests/omp-flow.test.ts`;
3. obsolete deployment path names in `src/cli/init.ts`, used only to delete old managed files;
4. the protected, non-imported/non-deployed/non-packaged `disposition.py` local historical source
   described above.

No permanent compatibility/migration allowlist was introduced.

## Independent-review repair

The first independent review correctly returned FAIL with three adapter/consumer blockers. The
needs-fix implementation repaired only those seams:

1. Canonical and deployed Claude research/architect/QbD/implement/check cards no longer require
   the deleted `<!-- omp-flow-claude-dispatch:v1 -->` prompt-replacement marker. Their Startup
   Gate now requires the exact strict-v1 `ompFlowDispatch` JSON from `operation start` as the first
   non-blank assignment line, checks the role expected by that card, and reads Bundle, entry,
   output, actor, receipt, predecessor, and predecessor output from the descriptor. The separate
   SubagentStart identity marker remains mandatory for exact agent type and non-empty native
   agent ID; it does not rewrite the assignment or replace descriptor actor identity.
2. `src/omp/extension.ts` now normalizes `qbd`/`qbd-auditor` strictly to `qbd-auditor` and
   recognizes `orchestrator`, completing the installed OMP role matrix. The focused suite starts
   a real operation for every managed OMP role
   (`architect`, `executor`, `explore`, `oracle`, `orchestrator`, `planner`, `qbd-auditor`,
   `researcher`, `reviewer`), forwards its complete assignment through `OMPFlowExtension`, and
   proves valid assignments remain byte-identical while malformed assignments block. An explicit
   missing-descriptor QbD probe also blocks.
3. Canonical plus `.agents`/OMP/Codex/Claude copies of `omp-flow-debug` and
   `omp-flow-ui-designer` now require Bundle root, entry Concept, output boundary, actor,
   operation receipt, and predecessor when applicable. They write linked handoff/diagnostic
   knowledge and contain no active row, context-pack, topology, phase, or Python Evidence
   semantics.
4. The suite now passes an actual `operation start` assignment into the Claude card startup
   contract, proves settings have no Agent/Task prompt interception, checks all five card roles
   and descriptor fields, verifies all ten debug/UI deployed copies, and exercises the complete
   OMP managed-role adapter matrix. This is the bounded static/isolated seam verification possible
   without claiming an uncaptured live Claude platform run.

Needs-fix verification:

- canonical/deployed card comparison — PASS, 5 pairs;
- canonical plus four project Skill roots — PASS, 10 debug/UI copies;
- retired Claude marker and debug/UI legacy-consumer search — PASS, zero matches;
- `npm test` — PASS, 184 checks;
- `npm run build`, Python compile, `git diff --check` — PASS;
- `npm pack --dry-run --json` — PASS, 86 files and zero legacy package paths.

## Remaining risk

No release blocker is known. The only deliberate residue is the protected local
`disposition.py` modification; because it cannot ship or deploy and has no importer, it does not
affect runtime behavior. Native Harness progress/concurrency/UI behavior was not changed.
