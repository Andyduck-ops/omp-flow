---
type: "Handoff"
title: "Codex apply_patch closed-grammar rework handoff"
---

# Codex apply_patch closed-grammar rework handoff

Status: `DONE`

This rework repairs the sole blocking finding in the
[independent Review](../review/codex-native-adapter-review.md) for
[Implement the native Codex adapter](codex-native-adapter.md). It is ready for a new
different-actor Review; this executor result is not reviewer acceptance.

## Delivered behavior

- The apply_patch envelope parser now rejects every unrecognized line beginning with `***`,
  including an unknown directive mixed into an otherwise safe recognized update.
- The closed grammar continues to accept Add/Update/Delete file directives, an Update-owned
  `Move to`, the single Begin/End envelope, exact `*** End of File`, and `@@` hunk markers.
- Unknown or broken directives use the existing fail-closed unverifiable-path denial; no runtime
  state, Hook configuration, installer behavior, or unrelated product scope changed.
- Canonical and repository-deployed `protect-runtime.py` remain byte-identical.

## Changed files

- `templates/codex/hooks/protect-runtime.py` — replace the partial known-prefix malformed check
  with a closed `***` directive check and preserve exact `*** End of File`.
- `.codex/hooks/protect-runtime.py` — byte-identical deployed synchronization.
- `tests/codex-hooks.test.py` — add mixed safe-plus-unknown/broken denial cases and positive
  coverage for Delete, Move, End-of-File, envelope, and hunk-marker compatibility.
- This handoff Concept.

All unrelated dirty and untracked files were preserved. No runtime/session record, Harness
configuration, package metadata, documentation, or non-Codex adapter file was modified by this
rework.

## Verification

- `python -X utf8 tests/codex-hooks.test.py -v` — PASS, 12 tests. The new mixed-directive test
  denies `Rename File`, a generic unknown directive, and a broken `Update Files` directive after a
  recognized safe update. Existing runtime, malformed-path, SessionStart, Hook config, Unicode,
  symlink, and real Windows launcher coverage remains green.
- `python -X utf8 -m py_compile templates/codex/hooks/protect-runtime.py .codex/hooks/protect-runtime.py`
  — PASS. Bytecode generated by this diagnostic was removed afterward.
- SHA-256 comparison — PASS; both guard copies are
  `F843BF9048806BC1F2B6206F2B3C8F2EBF8DBC2D00FE7E370CD14767ED935D12`.
- `git diff --check` — PASS; output contained only existing Git LF-to-CRLF working-copy warnings.

## Decisions and caveats

- Exact `*** End of File` is the only approved three-asterisk line inside the envelope that is
  not a file or move directive. Begin/End envelope lines remain validated separately, before the
  body scan.
- The parser still deliberately does not interpret hunk contents or authored Markdown semantics.
- The broader predecessor handoff's documented POSIX and real Codex `/hooks`/IDE/App evidence
  limitations are unchanged and were outside this narrowly authorized rework.
- Unproven rework done conditions: none. Independent acceptance remains pending.

## Operation correlation

- Actor ID: `hook_rework2`
- Dispatch receipt: `80ba07fdc31a4ec0ba007e2e45a3237f`
- Predecessor receipt: `bc7d6f72b1c640669a2f9df8734db1f7`
- Predecessor output: `.omp-flow/tasks/08-01-codex-native-hooks/work/codex-native-adapter-handoff.md`
- Output: `.omp-flow/tasks/08-01-codex-native-hooks/work/codex-native-adapter-rework-handoff.md`
